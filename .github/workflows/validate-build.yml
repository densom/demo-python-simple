name: Validate build

on: 
  push:
  pull_request:
  
jobs:
  validate-build:

    runs-on: windows-latest

    steps:

    - name: First job
      run: echo first job
    - name: Second job
      run: echo second job
